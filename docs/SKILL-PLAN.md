# SKILL-PLAN — skill-mcp v0.1.0

Greenfield package shipped on this box (Cloud Agents unavailable; visible Grok Build launcher hit Auto-review friction).

## Done

- B1 scaffold: package.json, tsconfig, MIT, gitignore
- B2 core: catalog / ranker / tokens, config roots, suggest+budget
- B3 bind state lean view + why
- B4 MCP stdio tools (list/suggest/bind/get/why/estimate/doctor/archive_idle/read_skill)
- B5 tests node:test (38 pass)
- B6 bilingual README + Claude Desktop / Cursor snippets

## Invariants

- Max 3; budget never raises cap
- No marketplace / auto-uninstall / ranker retrain
- Lean by default

## Out of scope

- registry publish, host adapters, binaries
